# Pinned exactly, not floored. The determinism claim depends on networkx's iteration order
# and on the scipy routine its eigenvector solvers call, so a version that merely satisfies
# a minimum would reproduce the results only by luck.
pandas==3.0.2
pyarrow==25.0.1
networkx==3.6.1
numpy==2.4.4
scipy==1.17.1
